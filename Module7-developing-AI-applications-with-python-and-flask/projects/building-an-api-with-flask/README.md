# Building an API with Flask: Route Creation, Error Handling, and HTTP Requests

```bash
pip install flask==2.2.2

pip install "Werkzeug>=2.2.2,<3.0"

flask --app server --debug run

curl -X GET -i localhost:5000
```